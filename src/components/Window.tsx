import { styled } from '~/theme'

export const Window = styled.article`
  font-family: Helvetica, sans-serif;
  font-size: 18px;
  padding: 10px 30px;
  line-height: 1.5em;

  button {
    font-size: inherit;
  }

  span[role='img'] {
    margin-bottom: -6px;
  }
`
